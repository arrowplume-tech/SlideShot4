# SlideShot 2.0

Приложение для конвертации HTML/CSS компонентов в PowerPoint презентации с нативными элементами.

## ⚠️ КРИТИЧЕСКИ ВАЖНО - ПЕРВЫЙ ШАГ ПРИ УСТАНОВКЕ

**При клонировании репозитория из GitHub ПЕРВЫМ ДЕЛОМ выполните:**

```bash
npm install
```

Это автоматически установит браузеры Playwright (Firefox), которые необходимы для корректной работы приложения.

### Почему это важно?

SlideShot использует Playwright для точного анализа HTML макетов. Без установленных браузеров:
- ❌ Приложение будет использовать JSDOM парсер (менее точный)
- ❌ Макеты будут конвертироваться с ошибками
- ✅ С Playwright - точная конвертация с сохранением всех стилей

## Установка и запуск

### 1. Установка зависимостей (ОБЯЗАТЕЛЬНО!)

```bash
npm install
```

После установки автоматически выполнится `postinstall` скрипт, который установит браузеры Playwright.

**Проверка установки:**
```bash
npm run verify-playwright
```

Если проверка успешна, вы увидите:
```
✅ Firefox успешно запущен!
✅ Страница создана!
✅ HTML рендеринг работает!
🎉 ВСЕ ПРОВЕРКИ ПРОЙДЕНЫ!
```

### 2. Настройка базы данных

```bash
npm run db:push
```

### 3. Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно на http://localhost:5000

### 4. Production сборка

```bash
npm run build
npm run start
```

## Технологии

- **Frontend**: React, TypeScript, Vite, Tailwind CSS, shadcn/ui
- **Backend**: Express, TypeScript
- **Database**: PostgreSQL + Drizzle ORM
- **HTML Parser**: Playwright (Firefox) + JSDOM fallback
- **PPTX Generation**: pptxgenjs

## Структура проекта

```
├── client/              # Frontend React приложение
│   └── src/
│       ├── components/  # UI компоненты
│       ├── pages/       # Страницы приложения
│       └── lib/         # Утилиты и хуки
├── server/              # Backend Express сервер
│   ├── lib/             # Библиотеки конвертации
│   ├── routes.ts        # API маршруты
│   └── index.ts         # Точка входа
└── shared/              # Общие типы и схемы
    └── schema.ts        # Drizzle схемы
```

## API

### POST /api/convert

Конвертирует HTML в PowerPoint презентацию.

**Request:**
```json
{
  "html": "<div>...</div>",
  "slideWidth": 10,
  "slideHeight": 7.5,
  "preserveImages": true,
  "optimizeShapes": true
}
```

**Response:**
```
Content-Type: application/vnd.openxmlformats-officedocument.presentationml.presentation
Binary PPTX file
```

## Переменные окружения

```env
NODE_ENV=development|production
DATABASE_URL=postgresql://...
```

## Устранение неполадок

### Playwright не работает

Если вы видите ошибки типа "Executable doesn't exist":

```bash
npx playwright install firefox
```

### База данных не подключается

Проверьте переменную окружения `DATABASE_URL` и выполните:

```bash
npm run db:push
```

## Лицензия

MIT
